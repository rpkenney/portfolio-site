# Stub

This page exists so the **writings** pipeline (Markdown → HTML) stays exercised. Replace it with real posts when you are ready.

- Indexed via `writings/index.yaml`
- First `#` heading becomes the page title (and is omitted from the body below)

```text
Build: python build.py
```
